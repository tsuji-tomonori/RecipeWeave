-- レシピ材料明細を作成する。
-- 値は名前付きパラメータで束縛する。
INSERT INTO recipeweave.recipe_ingredient AS t (
    id,
    recipe_version_id,
    line_no,
    form_id,
    product_version_id,
    component_id,
    kit_parent_line_id,
    role,
    demand_kind,
    amount_mode,
    amount,
    amount_max,
    unit_id,
    canonical_amount,
    conversion_id,
    scaling_rule_id,
    optional,
    note
)
VALUES (
    %(row_id)s,
    %(recipe_version_id)s,
    %(line_no)s,
    %(form_id)s,
    %(product_version_id)s,
    %(component_id)s,
    %(kit_parent_line_id)s,
    %(role)s,
    %(demand_kind)s,
    %(amount_mode)s,
    %(amount)s,
    %(amount_max)s,
    %(unit_id)s,
    %(canonical_amount)s,
    %(conversion_id)s,
    %(scaling_rule_id)s,
    %(optional)s,
    %(note)s
)
RETURNING
    t.id,
    t.created_at,
    t.recipe_version_id,
    t.line_no,
    t.form_id,
    t.product_version_id,
    t.component_id,
    t.kit_parent_line_id,
    t.role,
    t.demand_kind,
    t.amount_mode,
    t.amount,
    t.amount_max,
    t.unit_id,
    t.canonical_amount,
    t.conversion_id,
    t.scaling_rule_id,
    t.optional,
    t.note,
    t.xmin::TEXT AS etag;
